# Core automation
playwright>=1.44.0

# LLM backends (install at least one)
anthropic>=0.28.0
openai>=1.30.0

# Environment & utilities
python-dotenv>=1.0.0

# Web UI (phone control)
flask>=3.0.0

# HTTP API
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
aiofiles>=23.2.1          # required by FastAPI StaticFiles
httpx>=0.27.0             # Contentdrips API calls

# Python 3.10+ is required (no additional compat shims needed)
